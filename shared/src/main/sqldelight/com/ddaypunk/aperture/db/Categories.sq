CREATE TABLE categories (
  id INTEGER PRIMARY KEY NOT NULL,
  name TEXT NOT NULL
);

INSERT INTO categories (id, name)
VALUES (1, "Best Motion Picture of the Year");

selectAll:
SELECT * FROM categories;